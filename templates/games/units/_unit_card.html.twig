{% if unit is defined and unit %}
    <div class="unit-card">
        <div class="preview-box">
            <div class="sprite {{ unit.type }} {{ unit.color }} idle"></div>
        </div>
        <ul class="unit-stats">
            <li>Vitalité : {{ unit.vita }}</li>
            <li>Initiative : {{ unit.initiative }}</li>
            <li>Attaque : {{ unit.att }}</li>
            <li>Mana : {{ unit.mana }}</li>
        </ul>
        <p class="unit-desc">{{ unit.description }}</p>
    </div>
{% endif %}